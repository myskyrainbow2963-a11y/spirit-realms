# 四界灵契 · Godot 原生战斗原型

本项目已加入 Godot 4.7 原生战斗入口，保留原有网页版本（`index.html`）不受影响。

## 运行

在 Godot 4.7 中导入 `E:\kapian\project.godot`，然后按 `F6` 或 `F5` 运行。

也可从 PowerShell 启动：

```powershell
& 'E:\Program Files\game\Godot_v4.7.1-stable_win64.exe' --path 'E:\kapian'
```

## 当前原生实现

- 16:9 战场、动态环境光和漂浮粒子
- 玩家与三名敌人的场景站位、生命条、格挡和公开意图
- 扇形手牌、悬停抬升、能量、抽牌/弃牌堆
- 点击攻击牌后出现分段目标箭头，点击敌人结算伤害
- 敌方回合依次执行攻击、防御、减益；随后回到玩家回合
- 伤害文字、震屏和回合横幅
- 已接入新增素材：`card20` 霜牙圣狮、`card30` 幽冥骨翼、`card40` 月蚀灵狐、`card50` 深渊蛛皇、`card59` 雷霆豹
- 玩家角色状态机：待机呼吸与灵息飘带、攻击前冲挥剑、命中剑弧、受击后退；攻击伤害在动作命中帧结算
- AI 敌人池：`LittleMonster1–8` 随机组成两只小怪，`boss2–4` 随机生成一只大 Boss；均有待机、蓄力、攻击、受击、死亡动作反馈

主要代码在 `battle.gd`，场景入口为 `battle.tscn`。
